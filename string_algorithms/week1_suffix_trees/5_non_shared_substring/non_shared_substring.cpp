#include <iostream>
#include <string>

using std::string;

string solve(string p, string q) {
  string result = p;

  return result;
}

int main(void) {
  string p, q;
  std::cin >> p >> q;

  std::cout << solve(p, q) << std::endl;

  return 0;
}
